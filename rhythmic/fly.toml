app = 'rhythm'
primary_region = 'iad'

[build]

[deploy]
  # This runs your database migrations automatically before the app starts
  release_command = "node server/migrate.js"

[env]
  PORT = "8080"
  NODE_ENV = "production"

[http_service]
  internal_port = 8080
  force_https = true
  auto_stop_machines = true
  auto_start_machines = true
  min_machines_running = 0

[[vm]]
  memory = '1gb'
  cpu_kind = 'shared'
  cpus = 1
  memory_mb = 1024